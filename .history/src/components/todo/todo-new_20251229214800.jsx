const TodoNew = (props) => {
  // eslint-disable-next-line react/prop-types
  const { addNewTodo } = props;

  const handleClick = () => {
    const addbtn = 
  };
  return (
    <div className="todo-new">
      <input type="text" />
      <button id="addBtn" style={{cursor: ""}}>Add</button>
    </div>
  );
};

export default TodoNew;
